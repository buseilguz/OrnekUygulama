@model Sayfalar
@{
    ViewData["Title"] = "Home";
}

<div class="text-center">
    <h3 class="display-4">@Model.Baslik </h3>
    <div>
        @Model.Icerik
    </div>
    <div class="form-group">
        <button  value="Git" class="btn btn-primary" onclick="window.location.href = 'http://localhost:49762/Giris/GirisYap';" >Yönetici Paneli</button>
    </div>
</div>
