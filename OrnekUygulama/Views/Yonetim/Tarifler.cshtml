@model IEnumerable<OrnekUygulama.Models.Yemektarifleri>

@{
    ViewData["Title"] = "Tarifler";
    Layout = "~/Views/Shared/_yonetimtema.cshtml";
}


<p>
    <a asp-action="TarifEkle">Yeni Tarif Ekle</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
               Yemek Adı
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarif)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sira)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aktif)
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Kategori)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TarifId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Yemekadi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarif)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sira)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aktif)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Kategori.Kategoriadi)
            </td>
            <td>
                <a asp-action="TarifGetir" asp-route-id="@item.TarifId">Düzenle</a> |
                <a asp-action="TarifYorumlari" asp-route-id="@item.TarifId">Tarif Yorumları</a> |
                <a asp-action="TarifSil" asp-route-id="@item.TarifId" onclick="return confirm('Tarif Silinsin mi?')">Sil</a>
            </td>
        </tr>
}
    </tbody>
</table>
